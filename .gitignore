node_modules
.next
out
.env
*.db
*.db-journal
public/uploads/*
!public/uploads/.gitkeep
.DS_Store
.env*.local
.env.local
# Sentry Config File
.env.sentry-build-plugin

# Cache incrementale di TypeScript: locale alla macchina, non condivisibile.
*.tsbuildinfo

# Esiti di Playwright: rigenerati a ogni esecuzione, con dentro screenshot e
# tracce. Erano finiti nel repository perché non li aveva mai esclusi nessuno.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
e2e.txt

# Dati degli artisti in fase di reclutamento: contengono email e consensi di
# persone reali. Vedi RECLUTAMENTO.md.
/dati/
/consensi/
